git config --global user.name "username" [to add  ur username]
git config --global user.email "email" [to add  ur email]               in git  bush

git init [to initialize an empty git repository]

git stats [to check to untracked files]

git add -A     [to add untracked files to stagging area]

git commit -m "commit message required"   [to move staged files to local repositery]

git clone remote repositery exact copy local work space kittum

git log to get details

git branch is master branch

git push [localrepositery=>remote repositery]     git push remote_name branch[push]

git pull [remote repositery=>changes=>lws]

git remote add remote_name remote_path [adding remote]

git branch [to list all branch]

git remote -v [to list all remote]
